{
  "name": "flashmind-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --experimental-sqlite --experimental-strip-types --watch index.ts",
    "start": "node --experimental-sqlite --experimental-strip-types index.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.28.1",
    "msedge-tts": "^2.0.7"
  },
  "devDependencies": {
    "@types/node": "^22.7.5"
  }
}
